# DMA-BUF heap with CMA backing — required by rpicam-apps on RPi5.
# Without this /dev/dma_heap/linux,cma does not exist and rpicam-apps
# fails with "Could not open any dmaHeap device".
CONFIG_DMABUF_HEAPS=y
CONFIG_DMABUF_HEAPS_SYSTEM=y
CONFIG_DMABUF_HEAPS_CMA=y
